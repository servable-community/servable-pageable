//import performBatchOnQuery from "../../../utils/protocol-tools/performBatchOnQuery"

export default async ({ object }) => {
    // const versionableItems = object.relation('commentableEntries')
    // const query = versionableItems.query()
    // await Servable.App.Utils.performBatchOnQuery({
    //     query, action: async (i) => {
    //         await i.destroy({ useMasterKey: true })
    //     }
    // })
}